import { DetailInfoPipe } from "./detail-info.pipe";


describe('DetailInfoPipe', () => {
  it('create an instance', () => {
    const pipe = new DetailInfoPipe();
    expect(pipe).toBeTruthy();
  });
});
