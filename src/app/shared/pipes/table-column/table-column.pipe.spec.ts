import { TableColumnPipe } from './table-column.pipe';

describe('TableColumnPipe', () => {
  it('create an instance', () => {
    const pipe = new TableColumnPipe();
    expect(pipe).toBeTruthy();
  });
});
